import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    autosuggest: {
        position: absolute,
        top: '50',
        left: '9',
        zndex: 9999,
      
    },
    wrapper: {
        position: relative,
        height: '100%',
    }
});